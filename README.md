# qr-code-generator
In the spirit of being enough - I hate using websites for qr code generation. So i vibed this little helper. Build can run in windows.

Use setup.bat for pip install calls necessary

Use build.bat to run the build script the way I inteded it to be run.
